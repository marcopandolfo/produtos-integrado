module.exports = (app) => {
    const baseUrl = '/products';

    app.get(baseUrl, (req, res) => {
    });

    app.post(baseUrl, (req, res) => {
    });

    app.delete(baseUrl, (req, res) => {
    });
};
